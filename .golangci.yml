version: "2"
linters:
  enable:
    - unused
    - misspell
  disable:
    - errcheck
  settings:
    staticcheck:
      checks:
        - all
        - "-ST1000"
        - "-ST1001"
        - "-ST1003"
formatters:
  enable:
    - gofmt
    - goimports
